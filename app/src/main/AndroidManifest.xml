<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CleanArchitectureWithHiltMVVM"
        tools:targetApi="31">
        <activity
            android:name=".feature.presentation.imagesGallery.ImagesActivity"
            android:exported="false" />
        <activity
            android:name=".feature.presentation.moviesList.MoviesListActivity"
            android:exported="false" /> <!-- Activities -->
        <activity
            android:name=".core.navigation.RouteActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/AppTheme.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.presentation.movieDetails.MovieDetailsActivity"
            android:exported="true"
            android:label="@string/movies_screen_title" />
    </application>

</manifest>